<!DOCTYPE html>
<html>
  <head>
    <title>ReservationApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <!-- 11/21 11:20  この二行を追加 -->
  <% if user_signed_in? %>
    <header>
      <div><%= link_to "トップ", root_path, class: "text-white" %></div>
      <div><%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "text-white" %></div>
    </header>
    <% else %>
    <header>
      <div><%= link_to "新規会員登録", new_user_registration_path, class: "text-white" %></div>
      <div><%= link_to "ログイン", new_user_session_path, method: :delete, class: "text-white" %></div>
    </header>
  <% end %>

  <body>
  <!-- 11/20 16:52  この二行を追加 -->
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
